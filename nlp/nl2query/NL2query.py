
class NL2Query:

    def transform_nl2query(self, nlq: str, config: str = None) -> dict:
        """
        Takes a natural language query string and
        an optional config file with parameters.
        Transform the NL query into a structured query.
        Returns the equivalent structured query in a dict format.
        """
        pass
